// ──────────────────────────────────────────────────────────────────────────────
// Auto-generated by alu_bindgen for Alu Language
// Source Header: math_lib.h
// ──────────────────────────────────────────────────────────────────────────────

// Preprocessor Constants
namespace Constants {
    let MATH_PI: int = 3;
    let MAX_BUFFER_SIZE: int = 1024;
    let OPTION_FLAGS: int = 255;
}

// Enum: MathStatus
namespace MathStatus {
    let MATH_OK: int = 0;
    let MATH_ERR_DIV_ZERO: int = 1;
    let MATH_ERR_OVERFLOW: int = 2;
}

struct Point2D {
    int x;
    int y;
}

struct Sensor {
    int id;
    double reading;
}

// Extern C Routines
extern routine add_numbers(int a, int b) -> int;
extern routine multiply_floats(double a, double b) -> double;
extern routine compute_point_sum(Point2D p) -> int;
extern routine safe_divide(int a, int b, ptr<int> outResult) -> MathStatus;
extern routine reset_sensor(ptr<Sensor> s, int new_id, double new_reading) -> void;

// High-Level Idiomatic Wrappers
routine safe_add_numbers(a: int, b: int) -> int {
    return add_numbers(a, b);
}
routine safe_multiply_floats(a: double, b: double) -> double {
    return multiply_floats(a, b);
}
routine safe_compute_point_sum(p: Point2D) -> int {
    return compute_point_sum(p);
}
routine safe_safe_divide(a: int, b: int, outResult: ptr<int>) -> MathStatus {
    assert(outResult != null);
    return safe_divide(a, b, outResult);
}
routine safe_reset_sensor(s: ptr<Sensor>, new_id: int, new_reading: double) -> void {
    assert(s != null);
    reset_sensor(s, new_id, new_reading);
}
