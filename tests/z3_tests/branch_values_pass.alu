routine example(int flag) -> int {
 int values[2];
 int index = 4;
 if (flag > 0) { index = 0; } else { index = 1; }
 values[index] = 7;
 assert(index < 2);
 return 0;
}
routine main() -> int { return example(1); }
