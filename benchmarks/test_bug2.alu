import std::io;

routine fib(n: int) -> int {
    if (n <= 1) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

routine main() -> int {
    int result = fib(40);
    print_int(result);
    return 0;
}
